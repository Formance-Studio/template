# Route handlers are added here when the app needs data (§3.2).

# This folder stays empty until the first "save this" — an empty folder is a
# signal, not a stub. Adding a route means creating a file and mounting it in
# server.ts.
